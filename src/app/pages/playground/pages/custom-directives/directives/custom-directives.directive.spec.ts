import { CustomDirectivesDirective } from '../../../pages/custom-directives/directives/custom-directives.directive';

describe('CustomDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
